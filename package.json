{
  "name": "codecov-example-node",
  "version": "1.0.0",
  "description": "Codecov Example Node",
  "main": "app/calculator.js",
  "scripts": {
    "test": "c8 --reporter=lcov --reporter=text node --test --test-reporter=spec --test-reporter-destination=stdout --test-reporter=junit --test-reporter-destination=junit.xml"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/codecov/example-node.git"
  },
  "author": "Codecov <hello@codecov.io>",
  "license": "MIT",
  "devDependencies": {
    "c8": "^11.0.0"
  }
}
